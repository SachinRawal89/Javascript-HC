"use strict"; // terat all js code as newer version

//alert(1+1) // error because we are using node.js, not browser

let num = 11
let name = "sachin"
let state

let value = null

// boolean => true/fallse
// undefined => standalone value
// symbol => unique

console.table([num, name, state, value])
console.log(typeof value)
console.log(typeof state)